---
captured_by: specstudio:plan
status: queued
---
# Same-origin /api/* proxy as a fleet-wide pattern — Competios is the pilot; if it holds, expand to all Sneat sites to kill CORS preflights and make the API hostname a config detail rather than a published constant
